{
  "name": "simple-space-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel --no-cache --no-hmr ./src/index.html",
    "buildTreeShaking": "parcel build --experimental-scope-hoisting -d deploy ./src/index.html",
    "build": "parcel build -d deploy ./src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.25.1-test.1",
    "typescript": "^3.7.5"
  }
}
